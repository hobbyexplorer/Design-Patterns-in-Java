package model;

import static org.junit.Assert.*;

import org.junit.Test;

public class GameObjectRotateTest
{

	@Test
	public void testRotatedDimensions()
	{
		GameObject tester = new GameObject(0, 10, 10, 5, 20,1,1);
		//tester.rotatedDimensions(30);
		//assertEquals("height", 5, tester.getObjectHeight());
		//assertEquals("Width", 20, tester.getObjectWidth());
	}

}
